pcd_file: "./test_data/scene0010_00_37_pcd.ply" # path to pcd file
sample_number: 0 # down-sampling number when load pcd, <=0 means no down-sampling
sample_method: 'fps' # down-sampling method, ['random', 'fps']
sample_count: 256 # Specify the sample times when rendering, the higher the better
width: 1080 # rendered image width, default 1920
height: 1920 # rendered image height, default 1080
camera_file: './test_data/camera.xml' # set null to use default camera
camera_scale: 1.5
# camera_file: null
pts_size: 0.01 # point size of each point in pcd when rendering
pts_color: 'orange' # point color of each point in pcd when rendering